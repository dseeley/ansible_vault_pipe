#!groovy

node {
    stage('Call ansible/ansible_vault_pipe-release-tag') {
        build job: 'ansible/ansible_vault_pipe-release-tag', parameters: [string(name: 'NEW_VERSION', value: "next")]
    }
}